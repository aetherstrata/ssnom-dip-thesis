\ProvidesPackage{preamble}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{xargs}
\usepackage{babel}
\usepackage[autostyle]{csquotes}
\usepackage{amsmath,amsfonts,amsthm}%per gli ambienti matematici
\usepackage{graphicx}				%per le immagini raster
\usepackage{svg}					%per le immagini svg
\usepackage{booktabs}
\usepackage{gensymb}
\usepackage[
	backend=biber,
	citestyle=numeric,
	bibstyle=ieee,
	natbib=true,
	backend=biber,
	sorting=none,
	eprint=false
]{biblatex}
\usepackage{textcomp}
\usepackage{geometry}
\usepackage{verbatim}
\usepackage{makeidx}
\usepackage{color}
\usepackage{epstopdf}				%converte il eps in pdf
\usepackage{setspace}				%interlinea
\usepackage{float} 					%gestione figure
\usepackage{subcaption}				%gestione sottofigure
\usepackage{wrapfig}				%figure ai lati del testo
\usepackage{midpage}				%Posizionamento a centro pagina per avere Abstract ma anche numero pagine continuo
\usepackage{appendix}
\usepackage{hyperref}
\usepackage[acronym, section]{glossaries}
\usepackage{fancyhdr}
\usepackage{lipsum}
\usepackage[pdftex, dvipsnames]{xcolor,colortbl}

\usepackage[most]{tcolorbox}
\tcbuselibrary{skins,breakable}

\usepackage{tikz}
\usetikzlibrary{math}
\usetikzlibrary{calc}
\usetikzlibrary{shapes}
\usetikzlibrary{decorations.pathmorphing}

\setlength{\marginparwidth}{2.5cm}
\usepackage[colorinlistoftodos,prependcaption,textsize=tiny]{todonotes}

\newcommandx{\unsure}[2][1=]{\todo[linecolor=red,backgroundcolor=red!25,bordercolor=red,#1]{#2}}
\newcommandx{\change}[2][1=]{\todo[linecolor=blue,backgroundcolor=blue!25,bordercolor=blue,#1]{#2}}
\newcommandx{\info}[2][1=]{\todo[linecolor=OliveGreen,backgroundcolor=OliveGreen!25,bordercolor=OliveGreen,#1]{#2}}
\newcommandx{\improvement}[2][1=]{\todo[linecolor=Plum,backgroundcolor=Plum!25,bordercolor=Plum,#1]{#2}}

\newtcolorbox{note}[2][]{breakable,sharp corners, skin=enhancedmiddle jigsaw,parbox=false,
	boxrule=0mm,leftrule=2mm,boxsep=0mm,arc=0mm,outer arc=0mm,attach title to upper,
	after title={.\ }, coltitle=RawSienna,colback=yellow!10,colframe=Goldenrod, title={#2},
	fonttitle=\bfseries,#1
}

\newenvironment{bottompar}{\par\vspace*{\fill}}{\clearpage}

\linespread{1.5}
\geometry{a4paper, top=3cm, bottom=3cm, left=3.5cm, right=3cm,heightrounded,bindingoffset=5mm}

\hypersetup{
	colorlinks=true,
	linkcolor=black,
	citecolor = blue,
	pdftitle={\@title},
	pdfauthor={\@author}
}

\captionsetup{font={small}}

\pagestyle{fancy}

\renewcommand{\headrulewidth}{0.1 pt}
\renewcommand{\footrulewidth}{0pt}
\renewcommand{\arraystretch}{1.2}

\newcommand{\horrule}[1]{\rule{\linewidth}{#1}}					 % Create horizontal rule command with 1 argument of height

%\definecolor{LightGray}{gray}{0.9}

\makeglossaries
\loadglsentries{glossary}

\addbibresource{references.bib}

\renewcommand{\cite}[1]{\textsuperscript{[}\supercite{#1}\textsuperscript{]}}

\usepackage{subfiles}
