\ProvidesPackage{preamble}

\usepackage{babel}
\usepackage[autostyle]{csquotes}
\usepackage{amsmath,amsfonts,amsthm}%per gli ambienti matematici
\usepackage{graphicx}				%per le immagini raster
\usepackage{svg}					%per le immagini svg
\usepackage{booktabs}
\usepackage[
	backend=biber,
	citestyle=numeric,
	bibstyle=ieee,
	natbib=true,
	backend=biber,
]{biblatex}
\usepackage{textcomp}
\usepackage{geometry}
\usepackage{verbatim}
\usepackage{makeidx}
\usepackage{color}
\usepackage{epstopdf}				%converte il eps in pdf
\usepackage{setspace}				%interlinea
\usepackage{float} 					%gestione figure
\usepackage{subfig}					%gestione sottofigure
\usepackage{midpage}				%Posizionamento a centro pagina per avere Abstract ma anche numero pagine continuo
\usepackage{appendix}
\usepackage{hyperref}
\usepackage[toc, acronym]{glossaries}
\usepackage{fancyhdr}
\usepackage{lipsum}
\usepackage{xcolor,colortbl}

\newenvironment{bottompar}{\par\vspace*{\fill}}{\clearpage}

\linespread{1.5}

\geometry{a4paper, top=3cm, bottom=3cm, left=3.5cm, right=3cm,heightrounded,bindingoffset=5mm}

\hypersetup{
	colorlinks=true,
	linkcolor=black,
	citecolor = blue,
	pdftitle={\@title},
	pdfauthor={\@author}
}

\pagestyle{fancy}

\renewcommand{\headrulewidth}{0.1 pt}
\renewcommand{\footrulewidth}{0pt}
\renewcommand{\arraystretch}{1.2}

\newcommand{\horrule}[1]{\rule{\linewidth}{#1}}					 % Create horizontal rule command with 1 argument of height

%\definecolor{LightGray}{gray}{0.9}

\captionsetup{font={small}}			%Dimensione didascalie

\makeglossaries
\loadglsentries{glossary}

\addbibresource{references.bib}

\usepackage{subfiles}
